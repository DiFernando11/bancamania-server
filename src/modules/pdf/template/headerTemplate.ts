export const headerTemplate = ({ subtitle }: { subtitle: string }) => {
  return `<div class="header">
           <h1 class="title">BANCAMANIA</h1>
           <p class="subtitle">${subtitle}</p>
          </div>`
}
